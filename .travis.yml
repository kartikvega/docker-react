language: generic
sudo: required
services: 
  - docker

before_install:
  - docker build -t tkartik/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true tkartik/docker-react npm run test


